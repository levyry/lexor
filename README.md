# Lexor

An interactive, visual way to learn about SKI- and lambda-calculus.

## Build

This project uses [`trunk`](<github.com/trunk-rs/trunk>) for deployment. After installing that, simply run

```bash
trunk serve
```

to start a localhost instance.